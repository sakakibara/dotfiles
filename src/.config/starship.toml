command_timeout = 1000
add_newline = false

[directory]
truncate_to_repo = false

# Visible indicator that this shell is inside an agent-sandbox container.
# The container's entrypoint sets AGENT_SANDBOX=1; everywhere else the
# var is unset and the segment is hidden.
[custom.sandbox]
command = "echo '🛡 sandbox'"
when = '''test -n "$AGENT_SANDBOX"'''
format = "[$output]($style) "
style = "bold red"
# mox: when env=WSL_DISTRO_NAME and tool=starship.exe
[git_status]
windows_starship = "<machine.tool_path.starship.exe>"
# mox: end

# Windows packages -- bare names install via scoop's default bucket; entries
# of the form `bucket/name` use that scoop bucket; `winget:Pkg.Id` falls back
# to winget when scoop doesn't have the package. Profile gating uses the same
# trailing ` @profile1,profile2` syntax as the macOS / Linux files.
#
# Companion: etc/windows/packages-blacklist.txt (same syntax).
#
# Consumed by scripts/pre/os=windows/apps-scoop.ps1 on apply and reviewed by
# `dotfiles sync` (src/.local/bin/sync.ps1) on native Windows.

# Buckets (scoop bucket add)
# bucket:extras
# bucket:nerd-fonts

# CLI
# The gh shim (gh.cmd -> gh.ps1) hard-requires PowerShell 7.
pwsh
# neovim
# ripgrep
# fd
# bat
# fzf
# git
# gh

# GUI / casks
# extras/firefox
# extras/google-chrome
# extras/1password
# winget:Microsoft.PowerToys

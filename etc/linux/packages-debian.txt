# Native packages for Debian/Ubuntu. CI validates against debian:stable.
# Tools outside the default repos (gh, lazygit, lazydocker, starship) come
# from etc/bash/lib/tools.bash.
#
# Notes:
#   - bat may install as `batcat` on older Ubuntu/Debian (renamed due to
#     conflict with bacula-cli). The fd -> fdfind alias lives in .zshrc
#     and the prompt abbreviation in data/abbreviations.toml.
#   - gh is NOT in default Debian repos; handled by tools::gh.
#
# CLI tools and shells
bash
bat
fd-find
ffmpeg
fish
fzf
gawk
git
git-lfs
gnupg
imagemagick
jq
neovim
nnn
ripgrep
tree
wget
yt-dlp
zoxide
zsh

# Build tools
build-essential
autoconf
automake
bison
cmake
pkg-config
re2c

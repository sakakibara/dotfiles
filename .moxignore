# mox ignore (gitignore syntax; paths relative to $HOME).
# Keeps credentials and machine-local state out of the synced repo.
# Delete a line to track it (e.g. if the repo is private).

# Claude Code: only CLAUDE.md, settings.json, skills/, hooks/ are tracked
# (added explicitly). Everything else here is credentials, caches, and
# session data that must never be committed.
.claude/.credentials.json
.claude/*.jsonl
.claude/*-cache.json
.claude/settings.local.json
.claude/projects/
.claude/file-history/
.claude/sessions/
.claude/shell-snapshots/

# Codex: only AGENTS.md and hooks.json are tracked. Shared agent instructions,
# skills, and hook implementations live under .agents/.
.codex/auth.json
.codex/config.toml
.codex/*.json
.codex/*.jsonl
.codex/*.sqlite*
.codex/cache/
.codex/generated_images/
.codex/goals_*.sqlite*
.codex/history.jsonl
.codex/logs_*.sqlite*
.codex/memories_*.sqlite*
.codex/models_cache.json
.codex/sessions/
.codex/shell_snapshots/
.codex/state_*.sqlite*
.codex/tmp/

!.codex/hooks.json

# SSH / signing keys
.ssh/id_*
!.ssh/id_*.pub
*.pem

# Windows-only shims: PowerShell/batch scripts. On macOS/Linux the
# extensionless (bash) versions are used, so ignore these there.
# mox: when os=darwin or os=linux
*.ps1
*.cmd
# mox: end

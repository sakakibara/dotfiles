{{- /* Generated from .chezmoidata/abbreviations.toml — edit there, not here. */ -}}
{{- range .abbreviations -}}
{{- $shells := index . "shells" -}}
{{- $when := index . "when" -}}
{{- if and (or (not $shells) (has "fish" $shells)) (or (not $when) (lookPath $when)) }}
abbr -a -- {{ .key }} {{ .expansion }}
{{- end -}}
{{- end }}

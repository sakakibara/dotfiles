-- lua/lib/role.lua
-- Shipped fallback. chezmoi overwrites this file from a template
-- at dot_config/nvim/lua/lib/role.lua.tmpl. When chezmoi has not
-- applied, we return { kind = "private", is_work = false }.
return {
  kind = "private",
  is_work = false,
}
